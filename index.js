
var width = 400;
var height = 600;

window.setup = function () {

    createCanvas(width, height);
    document.documentElement.addEventListener("keydown", handleKeydown);

    console.log("Formation-JS-ISMIN");

};

window.draw = function () {

    background(0, 0, 0);

}

function handleKeydown() {

    if (key = " ") {}

};

require('p5');